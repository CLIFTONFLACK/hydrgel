node_modules
dist
dist-ssr
.vercel
*.local
.DS_Store
.env
.env.*

# Confidential source material must never enter this repo.
# The ACRA business profile contains shareholder personal data
# (residential addresses, NRIC / passport numbers).
*.pdf
*.docx

# Source snapshots. Written by scripts/fetch-sources.mjs on the runner and
# hydrated from the sources-cache branch by the newsroom routine — never
# committed to main.
.sources/
.sources-out/

# Build artefacts
tsconfig.tsbuildinfo
.claude/
